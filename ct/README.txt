Latex doc (see also 4coloring.wordpress.com):
- ct-ui-docs

Projects:
- ct-ui-core            Core java library for the theorem
- ct-ui-swixml          Swixml version (depends on G and swixml)

To finish:
- ct-ui-swing           TBF. Swing UI (depends only on java standard libraries)
  - I need to complete the migration to swing (from swixml) and remore the dependecy from G.jar (graphic lib)
- ct-ui-zk              TBF. ZK UI (depends on ZK + svg)
  - Web browser and jetty embedded within a swing application

Future projects:
- ct-ui-jmonkey         JMonkeyEngine is a Java 3D environment for gaming development
- ct-ui-javascript      A complete client side application

End of life:
- TBD
